# 开发进度与交接（PROGRESS）

> 最后更新：2026-09-03
> 本文件用于跨电脑继续开发：**先读本文件**，再继续按 TODO 推进。

## 一、项目是什么

**掼蛋大师训练营**：网页版掼蛋（Guandan）教学/对战游戏。
- 4 人两两组队（**对家为队友**，座位 0=你、2=对家（亮牌）、1=下家（右）、3=上家（左））
- 两副牌 108 张、27×4 全发、无底牌
- 完整规则：级牌（打几）、逢人配（两张红桃级牌为万能配）、顺子/连对/钢板/炸弹(4~8张)/同花顺/四王炸、A2345 与 10JQKA、从 2 打到 A、升级制（双上+3 / 头游+三游+2 / 头游+末游+1）、过 A 判定、接风、≤10 张报牌
- 纯前端零依赖：原生 HTML/CSS/JS，file:// 双击即玩；无构建、无 npm 包
- AI 教练侧栏（分析+可点选方案，两段式交互）+ 三档难度 BOT + 记牌器 + 音效（WebAudio 合成）+ 教程

## 二、当前进度状态（暂停节点 = 2026-09-03）

### ✅ 已完成并通过测试
- **逻辑层全部重写为掼蛋**（与原斗地主完全脱钩）：
  - `js/rules.js`（规则开关集中配置：a2345 / 王对 / 三带二带牌限制 / 三次不过回 2 / 报牌张数 / 进贡开关预留）
  - `js/cards.js`（两副牌生成，唯一 id = 点数-花色-副号）
  - `js/combos.js`（analyze 支持逢人配补位 / 同花顺 / 四王炸；beats 压制链：四王炸 > 8炸…6炸 > 同花顺 > 5炸 > 4炸；同顶同花顺按 ♠>♥>♣>♦）
  - `js/movegen.js`（先手/跟牌枚举，万能配受控展开，牌型+主值去重）
  - `js/game.js`（4 人 2 队引擎：排名、接风、升级/过 A/三不过回 2、全 Bot 自动续打；`autoNext` 可关）
  - `js/ai.js`（decomposeGuandan 拆解估算、bestPlay 高手决策、三档难度、队友让牌）
  - `js/advisor.js`（讲解/纠错/方案生成，推荐恒第一）
- **`node test/sim.js` 全绿**：36 项断言（牌型/压制链/万能配/A2345/走法合法性/教练方案/队友不压）+ 3 难度 × 20 手 4-BOT 对局仿真 + 1 整场（从 2 打到过 A）升级数学验证。
- **表现层已重构为 4 人对家对座**：`index.html` / `js/ui.js` / `css/style.css` / `js/app.js` 全部替换为掼蛋版（含主页/教程/本手结算/整场过 A 弹窗/彩带）。
- 目录内测试钩子：`?autotest=autoplay`（人类座位 AI 代打）、`&side=1`（强制展开教练侧栏）、`?debug=1`（输出主要元素包围盒，用于自动化检查重叠）。

### 🚧 已知问题（UI 打磨阶段，尚未完成）
> 以下均已在 844×390 / 956×440 / 1400×900 无头浏览器实测并留档（shots/d_*.html 为几何 dump）：

1. **对手竖向扇形过高**：左右对手座位（`.pseat.left/right` + `.p-fan.v`）纵向约 243~469px，桌面下顶到上方区域、窄屏下过高 → 需要限高/压缩（如 max-height + 双列或仅显示背面缩略）。
2. **记牌器与右侧元素**：`#counter`（约 284×82）在窄屏覆盖中部偏右，与右座/手牌区视觉拥挤 → 窄屏默认折叠为小 pill 或挪到左下方，需重调。
3. **中央信息**：`#center-msg` 宽度达 60%~83%，与座位文案可能交叠 → 收窄、居中避免覆盖；`#table-last` 需与出牌区错位。
4. **移动端抽屉开启时**（`&side=1` 验证过）：抽屉覆盖右侧属预期，但需确认遮罩与关闭手势；收起态下 `#coach-fab` 与 `#actions`（右下）间距复核。
5. **combo-label / me-bar / actions / play-0 纵向区**：底部四件套间距按桌面/横屏分别校准（几何见 dump，当前 me-bar y 与 hand 顶接近，视觉上需再留白）。
6. **对局视觉确认缺位**：尚未做一轮"出牌中/气泡/报牌/名次徽章/本手结算弹窗/整场彩带"的截图视觉验收。
7. **回归项待补**：教练两段式（选中→再点打出）、不自动金框、记牌器折叠、教程、音效开关在掼蛋版的手测清单。

## 三、后续 TODO（按依赖顺序）

- [ ] **UI 布局修复**（对应第二节已知问题 1~5）：
  - 对手扇形限高压缩；记牌器窄屏折叠与占位；中央文案收窄；底部四件套间距；桌面+横屏各一版几何复查（用 `?debug=1` + 截图）
- [ ] **视觉验收轮**：整局 autoplay 打到 handOver/sessionOver，截图核对 气泡/出牌区/名次/结算/彩带；做 1400、2560、956×440、844×390、竖屏提示 五档截图存档
- [ ] **交互回归手测清单**：两段式方案、展开分析才金框、双击单张、空格/P/Esc 快捷键、记牌器折叠、教练开关、音效开关、教程五 tab、退出/下一手/再来一场
- [ ] **体验细节**：报牌红点、接风提示气泡（"对家接风"）、本手结算增加"级牌推进"图示
- [ ] **规则增强（预留开关）**：`rules.gong=true` 进贡/还贡/抗贡 + 首手联动（引擎/UI 均预留了位置）
- [ ] **文档收尾**：README 最终版（截图、规则表、规则开关表）+ docs/ARCHITECTURE.md 掼蛋化收尾
- [ ] （可选）把 `shots/` 里留档的截图与 dump 清理或入库

## 四、怎么跑

```bash
# 方式 A：直接双击 index.html（file:// 已验证可用）
# 方式 B：本地静态服务器
npx http-server -p 8642 -c-1      # 访问 http://127.0.0.1:8642

# 逻辑测试（需 Node）
node test/sim.js                   # 36 断言 + 4BOT 仿真 + 整场升级
```

## 五、目录速览

```
js/rules.js   规则开关/级牌序/升级表（区域差异集中于此）
js/cards.js   两副牌 108 张
js/combos.js  牌型识别/压制（含逢人配）
js/movegen.js 走法枚举
js/game.js    4 人 2 队引擎（排名/接风/升级/过A）
js/ai.js      BOT（三档难度 + 队友配合）
js/advisor.js AI 教练（讲解/纠错/方案）
js/sfx.js     WebAudio 音效
js/ui.js      4 座渲染与交互
js/app.js     主页/教程/钩子
test/sim.js   逻辑测试与仿真
index.html / css/style.css
```
